# Tickedify Staging Environment Variables
# Use this template for dev.tickedify.com or staging deployment

# Staging Database (separate from production)
DATABASE_URL=postgresql://staging-username:password@staging-host:port/staging-database
# OR use separate Neon staging database:
POSTGRES_URL=postgresql://staging-connection-string
POSTGRES_PRISMA_URL=postgresql://staging-connection-string
POSTGRES_URL_NON_POOLING=postgresql://staging-connection-string

# Mailgun Configuration (can use same as prod with different route)
MAILGUN_API_KEY=your_mailgun_api_key
MAILGUN_DOMAIN=mg.tickedify.com

# Server Configuration
PORT=3000
NODE_ENV=staging

# Forensic Logging (enable for staging debugging)
FORENSIC_DEBUG=true

# Session Configuration
SESSION_SECRET=staging-session-secret-different-from-prod

# Admin Security (use different password for staging)
ADMIN_PASSWORD=staging-admin-password

# Staging Specific Settings
ENVIRONMENT_NAME=staging
STAGING_WARNING=true

# Notes:
# - Use separate database instance for staging to avoid prod data corruption
# - Enable FORENSIC_DEBUG=true for detailed staging logs
# - Use different passwords for security
# - Consider using same Mailgun domain with separate routing for staging emails